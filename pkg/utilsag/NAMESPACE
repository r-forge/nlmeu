# export(modifyPackageNamespace)
## export(.traceFunction, traceOptions, tracePrint, traceList)
# Generics for which we define new methods:
export(write.Rd, univariateStats)
export(utraceRinit, utraceRprint, utraceRlist)

S3method(write.Rd, data.frame)  # Writes skeleton documentation for data frame
